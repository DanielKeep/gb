
[../src/gb/io/Tail.d]
target=../bin/tailtool
buildflags+=-version=gb_io_Tail_tool

releaseflags+=-release -g -O -inline
debugflags+=-debug -g -unittest -version=Unittest

buildflags+=-I../src

[runtests.d]

